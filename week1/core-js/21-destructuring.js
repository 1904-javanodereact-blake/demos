let spaceShip = {
  name: 'Millenium Falcon',
  maxSpeed: 'can do kessel run in 12 parsecs', // in parsecs
  weight: 'heavy',
  passengerLoad: 20
}

let {name, weight} = spaceShip;

console.log(`name = ${name}, weight = ${weight}`);